services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: tcg
      POSTGRES_PASSWORD: tcg
      POSTGRES_DB: tcg_storefront
    volumes:
      - tcg_pg_data:/var/lib/postgresql/data

volumes:
  tcg_pg_data:
